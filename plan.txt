- ƒл€ переносимости убрать везде преобразование указателей к Word и UWord.
  ¬место этого ввести в basedefs.h операции выравнивани€ указател€ вперед и назад.

- Local interface может наследоватьс€ от unconstrained. Stub наследовать нельз€.

- Standard minor codes for system exceptions - использовать

- copy - ѕри пересечении областей DECOMMIT/RELEASE - ошибка. release нельз€ делать внутри
  copy_one_line, при многозадачной работе область могут зан€ть.

+ ѕовторный commit дл€ уже committed страниц - ошибка в логике.

- HeapDirectory::verify ()

- copy - ƒл€ небольших кусков пам€ти, пересекающих границу страницы,
  не всегда оптимальным €вл€етс€ выравнивание на то же смещение. Ќужно смотреть
	затраты.

- copy - —мена типа доступа на месте. ≈сли адреса совпадают и ничего не мен€етс€,
  ничего не делать.

- allocate - добавить флаг COPY_ALIGNED. ¬ыдел€ет блок пам€ти, выравненный так, чтобы
  оптимизировать копирование с адреса dst.

+ WinMemory::copy_one_line - заблокировать ремаппинг строки, если она содержит this.
  “ам же need_full_remap - зачем нужен, почему не CostOfOperation::remap_type (FULL_REMAP)?

